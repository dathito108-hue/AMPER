# AMPER AGI-Mobile Qualification Checkpoint — Phase566-570

## Scope

Phase566-570 continues the frozen completion campaign. It adds no new cognition, memory, planning,
authority or evolution subsystem. It qualifies two already-existing canonical capabilities:
SELF_LEARNING and SELF_EVOLUTION.

All workloads are synthetic and contain no user data. Evidence remains bound to the exact
AgiMobileQualificationSubject revision and artifact SHA-256.

## Phase566 — Real continual-learning transaction

The self-learning probe seeds governed synthetic Reflex experience and executes the real
ReflexNativeModelLifecycle. AMPER trains and activates an initial AMPER-owned System-1 champion through
the existing native training pipeline.

## Phase567 — Held-out retention after learning

Fresh web-search experience triggers the existing continual-learning path. The challenger must:

- be a new checkpoint;
- bind the previous champion as its parent;
- reuse historical replay examples;
- pass held-out admission and the existing anti-forgetting stability gate;
- become the active Reflex checkpoint.

Sixteen held-out device-status decisions are evaluated before and after replacement. The qualification
score is the retained fraction, and all 16 samples must remain correct for the reference workload.

## Phase568 — Closed-loop self-evolution transaction

Eight independent qualification samples execute the existing closed-loop self-evolution machinery.
Each sample begins from governed execution-reliability weakness and must traverse:

weakness evidence -> benchmark baseline -> inference blueprint -> sandbox evidence -> tournament ->
promotion ticket -> checkpoint -> exact apply -> fresh canary -> COMMITTED.

Sandbox and deployment implementations are synthetic qualification fixtures; the canonical campaign,
tournament, promotion executor, canary gate, ledger and deduplication logic are real.

## Phase569 — Transaction and replay invariants

Every self-evolution sample must:

- commit exactly one candidate;
- checkpoint exactly once;
- apply exactly once;
- require no rollback on the passing path;
- expose the exact promoted revision and artifact digest;
- persist a committed closed-loop evolution ledger entry;
- suppress an immediate retry from unchanged live evidence without applying twice.

The probe itself has no ToolFabric or AuthorityGate handle and cannot grant capability or approval.

## Phase570 — Runtime exposure and seven-domain checkpoint

AmperRuntime exposes agiMobileLearningEvolutionQualificationProbes(...).

The regression qualification run combines:

- Phase556-560 cognitive probes;
- Phase561-565 long-horizon + memory/world probes;
- Phase566-570 learning + evolution probes.

The canonical reference suite must qualify exactly seven domains:

1. SYSTEM1_FAST_PATH
2. NATIVE_SYSTEM2_REASONING
3. GENERALIZATION
4. LONG_HORIZON_EXECUTION
5. MEMORY_WORLD_MODEL
6. SELF_LEARNING
7. SELF_EVOLUTION

The project remains INCOMPLETE. The remaining domains are:

- MOBILE_RESOURCE_RESILIENCE
- RESTART_RECOVERY
- AUTHORITY_INVARIANTS

The next completion block must move to Android/device resource stress and restart/reboot recovery.
Authority-invariant attack qualification remains the final hard blocker before the full
amper-agi-mobile-v1 verdict can become QUALIFIED.
